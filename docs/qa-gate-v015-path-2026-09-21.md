# QA GATE v0.1.5-path — 2026-09-21

Measured results only. Project: `/workspace/tower-of-darkness`.

## APK

| Field | Value |
|-------|-------|
| Path | `/workspace/tower-of-darkness/tower-of-darkness-debug.apk` |
| Size | 17709345 bytes (16.89 MiB) |
| mtime | 2026-09-21 13:05:17 EDT |

## Path generator 100-floor sim

- **API:** `PathGenerator.generate(floor = 1, rng = Random(seed))`
- **Seeds:** `0..99` inclusive (100 floors)
- **Combat count definition:** all nodes with `type == COMBAT` that are neither `START` nor `BOSS` (mid-branch + merge if combat)
- **Test:** `app/src/test/java/com/towerofdarkness/app/PathGateV015Test.kt` → `pathGate_v015_100floorSim_seeds0to99`

### Combat before boss

| Metric | Value |
|--------|-------|
| `floors_with_zero_combat_before_boss` | **0** |
| combat histogram | `{1=20, 2=42, 3=25, 4=12, 5=1}` |

CoS criterion **0 floors with 0 combats before boss:** **PASS**

### Events per floor

| Metric | Value |
|--------|-------|
| event histogram | `{0=18, 1=82}` |
| `event_max` | 1 |
| floors with events > 1 | **0** |

Criterion **events ≤ 1 per floor:** **PASS**

### Outliers

| Metric | Value |
|--------|-------|
| `outliers_count` | **0** |

No structural outliers (missing merge/boss, wrong start/boss count, unreachable boss, unusual mid-branch counts outside 4–9, zero-combat, or multi-event).

## Gradle unit tests

Command:

```bash
./gradlew testDebugUnitTest \
  --tests com.towerofdarkness.app.WeaponXpTest \
  --tests com.towerofdarkness.app.PathGeneratorTest \
  --tests com.towerofdarkness.app.PathGateV015Test
```

| Suite | Tests | Failures | Errors | Result |
|-------|-------|----------|--------|--------|
| `WeaponXpTest` | 4 | 0 | 0 | **PASS** |
| `PathGeneratorTest` | 4 | 0 | 0 | **PASS** |
| `PathGateV015Test` | 1 | 0 | 0 | **PASS** |

Overall Gradle: **BUILD SUCCESSFUL**

## WeaponXpTest coverage (from source)

File: `app/src/test/java/com/towerofdarkness/app/WeaponXpTest.kt`

1. **`nextWeaponLevel_fullWakeWin_levelsUp`** — `GameController.nextWeaponLevelAfterFight(level, fullWake=true, killed=false, won=true)` → Lv1→2, Lv2→3, Lv3 stays 3.
2. **`nextWeaponLevel_noWake_noLevel`** — full wake false → stays Lv1.
3. **`nextWeaponLevel_loss_noLevel`** — playerWon false → stays Lv1.
4. **`spinnerWin_fullWake_ashbrandBecomesLv2_beforeNextNode`** — CombatEngine vs `EnemyKind.SPIDER` ("Seal Spinner"), Ashbrand Lv1 charge 3, forced `pendingFullWake` / `awaitingWeapon`, resolve weapon then dice/skills until win; asserts `fullProcThisCombat`, win, and `nextWeaponLevelAfterFight(...) == 2` ("Ashbrand must be Lv2 before Warden").

Confirms full Wake + combat win → +1 weapon level before next node (Spinner → Warden Lv2 path). Does **not** retune Wake/counters/HP.

Helper under test (`GameController.nextWeaponLevelAfterFight`): levels only on win when `fullProcThisCombat || fullProcKilled`, capped at 3.

## Gate verdict

**PASS** — zero combat-before-boss floors = 0; events ≤1 on all 100 seeds; no outliers; WeaponXpTest + PathGeneratorTest + PathGateV015Test all green.

