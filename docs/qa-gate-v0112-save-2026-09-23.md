# QA Gate: v0.1.12-save (Compose Android)

**Date:** 2026-09-23 (America/New_York, EDT)  
**Project root:** `/workspace/tower-of-darkness`  
**Scope:** Measure-only. Do **not** retune 2x timings / path / boss-rest / persist rules / Wake / counters / shop / swap / Scout.  
**Hard exclusion:** Godot / tower-of-the-world / verify_v032 not touched or cited.  
**Claimed engineer tip:** `1f06b15562c3687a0327f2ad743eeaaff1ccdff5` on `main`

---

## QA GATE PASS / GREEN LIGHT

---

## 1) APK + git

| Item | Measured |
|------|----------|
| APK path (root copy) | `/workspace/tower-of-darkness/tower-of-darkness-debug.apk` |
| APK path (build) | `/workspace/tower-of-darkness/app/build/outputs/apk/debug/app-debug.apk` |
| APK size | **17762468** bytes (both copies; md5 `cf40b66731942e17f89a242898fdc18a` identical) |
| APK mtime (root) | **2026-09-23 11:56:53.512274136 -0400** (EDT) |
| APK mtime (build) | **2026-09-23 11:56:53.388274073 -0400** (EDT) |
| `git log -1` | `1f06b15 v0.1.12-save: one mid-run DataStore slot; Path/FloorBreak resume` |
| `git rev-parse HEAD` | `1f06b15562c3687a0327f2ad743eeaaff1ccdff5` |
| HEAD vs claimed | **Matches** tip `1f06b15…` |
| Commit date | 2026-09-23 11:57:01 -0400 (EDT) |
| Branch status | `main...origin/main` |

### `git show --stat HEAD` (files touched)

```
README.md
app/src/main/java/.../MainActivity.kt
app/src/main/java/.../data/MetaStore.kt
app/src/main/java/.../data/MidRunSlot.kt          (+533)
app/src/main/java/.../nav/GameController.kt
app/src/main/java/.../ui/screens/MainMenuScreen.kt
app/src/test/java/.../MidRunSaveV0112Test.kt      (+175)
docs/meta-economy-v0.md
docs/midrun-save-v0112.md
docs/save-v0112.md
docs/slice-screens.md
11 files changed, 1260 insertions(+), 58 deletions(-)
```

Kotlin sources only: 6 files, +994 / −13.

---

## 2) Unit tests (QA re-run)

Command:

```bash
./gradlew :app:testDebugUnitTest \
  --tests 'com.towerofdarkness.app.MidRunSaveV0112Test'
```

**BUILD SUCCESSFUL** (`:app:testDebugUnitTest` executed). Measured from  
`app/build/test-results/testDebugUnitTest/TEST-com.towerofdarkness.app.MidRunSaveV0112Test.xml`  
(timestamp `2026-09-23T15:59:21` Z ≈ **11:59:21 EDT**): **tests=8 failures=0 errors=0 skipped=0 time=0.078s**.

### MidRunSaveV0112Test — 8/8 PASS

| Test | Result |
|------|--------|
| `roundTrip_preservesFloor2HpRemLoadoutAshbrandClearedAndRngSeed` | PASS |
| `pendingStair_resumeIsFloorBreak_notPath` | PASS |
| `stairContinuePayload_floor2_clearsPending_resumesPath` | PASS |
| `killWardenThenForceStop_onStair_resumesFloorBreak_notF1Path` | PASS |
| `corruptOrWrongSchema_decodeReturnsNull` | PASS |
| `rngSeed_requiredAndRoundTrips` | PASS |
| `newClimbWipe_meansNoActiveSlot` | PASS |
| `noCombatSnapshotFields_inEncodedPayload` | PASS |

Discovery (`find` / path scan): sole save/resume suite is `MidRunSaveV0112Test.kt`. Implementation: `MidRunSlot.kt`, `MetaStore` mid-run APIs, `GameController` persist/resume, `MainMenuScreen` Continue.

---

## 3) Required verdicts a–e (measured)

### a) Mid Floor 2 force-stop/reopen → same floor, HP/rem/loadout/Ashbrand, cleared nodes — **PASS**

**Evidence (test):** `roundTrip_preservesFloor2HpRemLoadoutAshbrandClearedAndRngSeed` asserts after encode/decode: `floor==2`, `playerHp==17`, `runWallet==14`, loadout locked + card ids, `ashbrand.level==2`, `path.cleared` non-empty, node count preserved, `toTowerPath` floor/cursor/cleared set match.

**Evidence (code):** `GameController.init` / `continueClimb` → `applyMidRunSlot(slot)` then `nav = MidRunSlot.resumeNav(slot)` (Path when not pending stair). Persist at node resolve via `returnToPathAfterResolve` → `persistMidRun(pendingStair=false, resume=Path)`.

### b) Kill Warden, force-stop on "The stair turns." (`pending_stair`) → FloorBreak; or F2 path → F2 Path — NOT F1 — **PASS**

**Evidence (tests):**

- `killWardenThenForceStop_onStair_resumesFloorBreak_notF1Path` → `resumeNav == FloorBreak`, `floor==1`, `pendingStairContinue==true`
- `pendingStair_resumeIsFloorBreak_notPath` → encode/decode keeps flag; `resumeNav == FloorBreak`
- `stairContinuePayload_floor2_clearsPending_resumesPath` → `pendingStairContinue==false`, `resumeNav == Path`, `floor==2` and `path.floor==2`

**Evidence (code):** Dual write #1 in `onCombatEnd` boss win → `FLOOR_BREAK`: `persistMidRun(pendingStair=true, resume=FloorBreak)`. Dual write #2 in `continueAfterFloorBreak`: generates floor 2 path, `persistMidRun(pendingStair=false, resume=Path)`. `MidRunSlot.resumeNav`: pending OR FloorBreak → `NavState.FloorBreak`, else `Path`.

### c) New climb after confirm → fresh Floor 1, old slot gone — **PASS**

**Evidence (test):** `newClimbWipe_meansNoActiveSlot` — wiped `null` → `isActiveClimb==false`; active slot → true.

**Evidence (code):** `confirmNewClimb()` → `meta.clearMidRunSlot()`, `hasMidRunSlot=false`, `beginClimbFresh()` → `startNewRun()` (floor 1 path). Menu: with slot shows "New climb" confirm dialog; wipe copy states remnants bank/perks stay.

### d) No save written during in-flight Wake — **PASS**

**Evidence (test):** `noCombatSnapshotFields_inEncodedPayload` — encoded JSON must **not** contain `enemy_hp`, `"round"`, `dice`, or `wake`.

**Evidence (code):** `persistMidRun` call sites only: loadout confirm on Path (L348), first loadout lock leave (L382), `returnToPathAfterResolve` (L713), boss → FloorBreak (L730), stair Continue → F2 (L761). **None** inside `runCombatBeats` / Wake hold (`pendingFullWake` / `WEAPON_FULL_HOLD_MS` path L439–445). Combat ends await Continue; persist happens after `continueAfterCombat` → `onCombatEnd` (post-fight), not mid-Wake.

### e) Continue visible iff slot exists; `rng_seed` present; dual stair write behaves — **PASS**

**Evidence (UI):** `MainMenuScreen`: `if (gc.hasMidRunSlot) { Button Continue + New climb } else { Button Climb }`. `hasMidRunSlot` set from `meta.readMidRunSlot() != null` on boot and on continue/clear.

**Evidence (rng_seed):** `rngSeed_requiredAndRoundTrips` asserts `"rng_seed":` in JSON and round-trip; `corruptOrWrongSchema_decodeReturnsNull` rejects schema-only payload missing `rng_seed`.

**Evidence (dual stair):** GameController comments + calls at L730 (write #1 pending) and L761 (write #2 clear pending / F2 Path); covered by tests in (b).

---

## 4) Frozen check

| System | Verdict | Evidence |
|--------|---------|----------|
| 2x timings | Untouched | No domain/Balance/CombatSpeed diff; `combatSpeed2x` only snapshotted in slot |
| Path generation rules | Untouched | `app/.../domain/` **empty** vs `HEAD~1`; GC only wraps `PathGenerator.generate` with `MidRunSlot.floorSeed` |
| Boss-rest | Untouched | No BossRest / Rest screen / Balance rest diffs |
| Persist (prior meta) | Meta bank/perks not rewritten for balance | Mid-run slot APIs added; `confirmNewClimb` explicitly leaves remnants/perks |
| Wake | Untouched | Wake hold still `Balance.WEAPON_FULL_HOLD_MS`; no persist mid-Wake |
| Counters / shop / swap / Scout | Untouched | No Shop/Treasure/Scout domain or prior unit-test file changes; shop visits / scout charges snapshotted only |

`git diff --name-only HEAD~1 HEAD` under `domain/` and prior Combat*/Boss*/Path*/Shop*/Treasure*/Floor*/Weapon* tests: **empty**.

---

## 5) Doc path

`/workspace/tower-of-darkness/docs/qa-gate-v0112-save-2026-09-23.md`

---

## Overall

**Gate PASS** — HEAD matches claimed tip; APK rebuilt (17762468 B @ 11:56:53 EDT); `MidRunSaveV0112Test` 8/8 PASS on QA re-run; cases a–e PASS with test + code evidence; frozen systems not retuned.
