# QA Gate: v0.1.11-2x (Compose Android)

**Date:** 2026-09-23 (America/New_York, EDT)  
**Project root:** `/workspace/tower-of-darkness`  
**Scope:** Measure-only. Do **not** retune path / rest-before-boss / persist / Wake rules / counters / HP / shop / swap / Scout.  
**Hard exclusion:** Godot / tower-of-the-world / verify_v032 not touched or cited.  
**Claimed engineer tip:** `2a59813` on `main` (base `ab512d8`)

---

## QA GATE PASS / GREEN LIGHT

---

## 1) APK + git

| Item | Measured |
|------|----------|
| APK path (build) | `/workspace/tower-of-darkness/app/build/outputs/apk/debug/app-debug.apk` |
| APK path (root copy) | `/workspace/tower-of-darkness/tower-of-darkness-debug.apk` |
| APK size | **17718534** bytes (both copies; md5 `c91767a0320d8b17cdd31f7204a1112f` identical) |
| APK mtime (build) | **2026-09-23 10:47:43.262108251 -0400** (EDT) |
| APK mtime (root copy) | **2026-09-23 10:47:43.546107709 -0400** (EDT) |
| `git log -1` | `2a59813 v0.1.11-2x: combat 1x↔2x toggle; holds ×0.5 at 2x` |
| `git rev-parse HEAD` | `2a59813faa51d353acc997a059d6b23de5127f29` |
| HEAD vs claimed | **Matches** tip `2a59813` |
| Parent / base | `ab512d8eb0d861c2e1ba94fb5df7d9d5d9dbc530` (`ab512d8 release: v0.1.10-bossrest…`) |
| Branch status | `main...origin/main` |

### `git show --stat 2a59813` (files touched)

```
README.md
app/src/main/java/com/towerofdarkness/app/domain/Balance.kt   (comment only)
app/src/main/java/com/towerofdarkness/app/nav/GameController.kt
app/src/main/java/com/towerofdarkness/app/ui/screens/CombatScreen.kt
app/src/test/java/com/towerofdarkness/app/CombatSpeedV0111Test.kt
docs/combat-2x-v0111.md
docs/combat-bar-v013.md
7 files changed, 173 insertions(+), 14 deletions(-)
```

---

## 2) Unit tests (QA re-run)

Command:

```bash
./gradlew :app:testDebugUnitTest \
  --tests 'com.towerofdarkness.app.CombatSpeedV0111Test' \
  --tests '*Wake*' \
  --tests '*Combat*'
```

**BUILD SUCCESSFUL** (Gradle daemon; `:app:testDebugUnitTest` executed). Measured from `app/build/test-results/testDebugUnitTest/TEST-*.xml` (timestamp `2026-09-23T14:49:49` Z ≈ **10:49:49 EDT**).

### CombatSpeedV0111Test — 5/5 PASS (0 failures)

| Test | Result |
|------|--------|
| `holdMs_1x_keepsBudgets` | PASS |
| `holdMs_2x_halvesEveryHold` | PASS |
| `holdMs_clampsAbove2_no3x` | PASS |
| `holdMs_zeroOrNegativeSpeed_treatedAs1x` | PASS |
| `wakeReadableAt1x_andHalfAt2x` | PASS |

### Related combat / Wake suites also PASS in this run

| Suite | tests | failures |
|-------|------:|---------:|
| `CombatEngineTest` | 12 | 0 |
| `CombatSimV013Test` | 1 | 0 |
| `CombatSimV014Test` | 1 | 0 |
| `CombatSpeedV0111Test` | 5 | 0 |
| `Floor2Test` | 1 | 0 |
| `PathGeneratorTest` | 3 | 0 |
| `WeaponXpTest` | 3 | 0 |

Notable Wake/gold asserts still PASS (engine untouched by tip):

- `CombatEngineTest.wake_exactLogLine_andSparkNoGoldAnim` — `ASHBRAND — WAKE 4`, `goldLog=true`, float `WAKE`
- `CombatSimV014Test.simV014_headless_report` — FULL Wake gold / SPARK no-gold asserts

---

## 3) Required verdicts (measured)

### a) 1x: Wake still readable, gold line pinned — **PASS**

| Evidence | Detail |
|----------|--------|
| Hold budgets @1x | `combatHoldMs(base, 1) == base` (`holdMs_1x_keepsBudgets`) |
| Wake hold @1x | `WEAPON_FULL_HOLD_MS` → **2300 ms** (`wakeReadableAt1x_andHalfAt2x`) |
| Common beat @1x | `SKILL_COMMON_MS` → **1700 ms** (same test) |
| Gold Wake line | `CombatEngine` still emits `ASHBRAND — WAKE $dmg` with `goldLog=true`, `pinnedWakeLine` set; tip did **not** modify `CombatEngine.kt` |
| UI pin | `CombatScreen` still injects `pinnedWakeLine` into recent log with `goldLog=true` and paints gold (unchanged by tip except VFX delay scaling) |
| Engine tests | `wake_exactLogLine_andSparkNoGoldAnim` PASS |

### b) 2x: same fight ~half wall time; log order unchanged — **PASS**

Measured holds from `CombatSpeedV0111Test` + `GameController.combatHoldMs` (`speedX >= 2` → divide by 2, else 1):

| Hold constant | 1x ms | 2x ms | ratio |
|---------------|------:|------:|------:|
| `DICE_MS` | 800 | 400 | 0.5 |
| `SKILL_COMMON_MS` | 1700 | 850 | 0.5 |
| `SKILL_RARE_MS` | 2300 | 1150 | 0.5 |
| `READ_HOLD_MS` | 1800 | 900 | 0.5 |
| `WEAPON_HOLD_MS` | 1500 | 750 | 0.5 |
| `WEAPON_FULL_HOLD_MS` (Wake) | 2300 | 1150 | 0.5 |
| `ENEMY_HOLD_MS` | 1500 | 750 | 0.5 |

Sample one-round hold sum (dice + common skill + read + FULL Wake + enemy) derived from those measured values:

| Mode | Total hold ms | ratio vs 1x |
|------|-------------:|------------:|
| 1x | **8100** | 1.0 |
| 2x | **4050** | **0.5** |

`wakeReadableAt1x_andHalfAt2x` also asserts `wake2 * 2 == wake1` (2300 / 1150).

**Log order / no skipped beats:** tip replaces each `delay(Balance.*)` with `combatHold(Balance.*)` in the same sequence (dice → skill → read → weapon → enemy → readyNext). No beats removed; no skip control. `CombatEngine` resolution order unchanged (not in tip diff).

### c) Mid-fight toggle → NEXT beat only — **PASS**

| Mechanism | Location |
|-----------|----------|
| Toggle | `GameController.toggleCombatSpeed()` flips `combatSpeedX` 1↔2 only |
| Hold commit | `combatHold(baseMs)` calls `delay(combatHoldMs(baseMs, combatSpeedX))` — speed read **when the hold starts**; in-flight `delay` already committed |
| Doc comment | “Snapshots [combatSpeedX] when the hold starts so a mid-fight toggle only affects the NEXT beat” |
| UI VFX | `CombatScreen` LaunchedEffect keys on `state?.log?.size` and samples `gc.combatSpeedX` at beat start for dice/float ticks |

No unit test directly flips mid-hold; mechanism evidence is code + comments above.

### d) No 3x; label / combatHoldMs show active 1x or 2x — **PASS**

| Check | Evidence |
|-------|----------|
| Clamp | `combatHoldMs`: `val x = if (speedX >= 2) 2 else 1` — speed 3 and 99 equal speed 2 (`holdMs_clampsAbove2_no3x` PASS) |
| Toggle states | `toggleCombatSpeed`: only `1 → 2` or `2 → 1` |
| UI label | `CombatScreen`: `Text("${gc.combatSpeedX}x")` — shows **active** rate |
| No skip | Tip message / docs: no skip-animations control |

### e) Frozen check — **PASS**

Tip subject is combat 1x↔2x hold scaling + toggle UI only.

| Frozen system | In tip diff? |
|---------------|--------------|
| Path generator / rest-before-boss | **No** |
| F2 persist | **No** |
| Wake / CHAIN / SPARK **rules** (`CombatEngine`) | **No** (presentation length scales via holds only) |
| Counters / HP | **No** (`Balance` numeric values unchanged; comment-only edit) |
| Shop / swap | **No** |
| Keen Eye Free Scout | **No** |

Touched: `GameController` hold wrappers + `combatSpeedX`, `CombatScreen` toggle/VFX timing, `Balance` comment, new `CombatSpeedV0111Test` + docs/README.

---

## 4) Overall

**GATE PASS / GREEN LIGHT** — HEAD `2a59813`, APK 17718534 bytes @ 2026-09-23 10:47:43 EDT, CombatSpeed 5/5 + related combat/Wake suites PASS, 1x Wake 2300 ms + gold pin intact, 2x holds exactly ×0.5 (sample round 8100→4050 ms), mid-fight NEXT-beat via hold snapshot, no 3x, frozen systems not tip subject.
