# Hub — v0.1.27-hub (WO LOCKED, packs PASS)

Status: **locked** (CoS / Elliott). Owner: **Meta & Ops** (remnants / unlocks / hub economy).  
Engineer implements UI + persist; Architect owns skill combat rules once unlocked.

**Scope:** Tower of Darkness is greenfield. Other projects are out of scope.

**Supersedes** `meta-economy-v0.md` §3 Hub ladder **for this WO only** (four offers below). Path Shop rotation + earn table unchanged. No economy retune beyond these Hub offers.

---

## Screen

| Element | Spec |
|---------|------|
| Title | **Hub** |
| Top bank line | `Remnants  N` (same `remnants_bank` as title form `Hub · N remnants`) |
| Offers | **4** rows: name, one-line effect, cost |
| Row CTA | **Buy** / **OWNED** / **Can't afford** |
| Nav | **Back to title** (Menu) |

Buy spends bank **immediately**. No Path-style rotating shop stock on Hub.

---

## Offers (persist across runs)

| # | Title | unlock_id | Cost | Effect (Hub one-liner) | Grant / unlock |
|---|-------|-----------|------|------------------------|----------------|
| 1 | **Scout** | `scout_charge` | **8** | +1 Free Scout / climb | +1 `freeScoutCharges` at climb start (stack with existing Keen Eye grant sites) |
| 2 | **Extra rumor** | `extra_rumor` | **6** | +1 rumor re-roll / floor | +1 `rumorRerolls` **per floor** (F1 start + FloorBreak→F2); separate from any climb-only grant |
| 3 | **Host of Embers** | `host_of_embers` | **12** | Unlock skill Cinder Vow | Adds **Cinder Vow** to climb card pool when owned |
| 4 | **Iron Lesson** | `iron_lesson` | **12** | Unlock skill Grave Nail | Adds **Grave Nail** to climb card pool when owned |

All four always listed (owned rows show **OWNED**, not rebuy). Cheapest unowned with bank ≥ cost is the useful spend; if bank &lt; all remaining, Climb/title earn-path stays available (no meta stall).

**Costs are WO overrides** of the older ≥15 cheapest-unlock lock. Do not invent other Hub prices this pass.

---

## Skill economy wording (lock; Engineer implements combat)

| Skill | Title source | Rarity / weight | Combat text (lock) |
|-------|--------------|-----------------|--------------------|
| **Cinder Vow** | Host of Embers | Uncommon **w3** | Deal **5**. If Ashbrand has ≥1 pip, gain Brace **2**. |
| **Grave Nail** | Iron Lesson | Uncommon **w3** | Deal **4**. Next enemy counter **−1** (min **1**). Soften **1**. |

Skills appear in loadout/pool **only after** their Hub unlock. No Hub sale of open cards #1–9.

---

## Persist (`meta_v0` — existing store)

Climb-save this WO: **read rem bank only** — **no** rewrite of mid-climb schema beyond rem read (`midrun-save-v0112.md` otherwise frozen).

```
meta_v0 {
  remnants_bank: int              // spend on Buy; persist remaining
  unlocks: string[]               // include scout_charge | extra_rumor | host_of_embers | iron_lesson when owned
  // …other existing meta keys unchanged
}
```

| Event | Persist |
|-------|---------|
| Buy | `remnants_bank -= cost`; append `unlock_id` to `unlocks` |
| Summary → Hub | Bank run wallet into `remnants_bank` (existing); clear mid-run slot |
| New climb | Apply owned Hub grants to climb wallets (Scout / Extra rumor rules above); do not wipe owned flags |
| Mid-run resume | Read bank if UI needs it; **do not** invent new midrun fields for Hub |

---

## Frozen this WO

- Combat art, Wake, path weights, 2x
- Mid-climb save schema (beyond rem bank **read**)
- Path Shop catalog / earn table
- Packs (v0.1.26 PASS)

---

## Engineer checklist

- [ ] Hub title + `Remnants  N` + 4 offers + Buy / OWNED / Can't afford + Back to title
- [ ] Costs: Scout **8**, Extra rumor **6**, Host of Embers **12**, Iron Lesson **12**
- [ ] Immediate bank spend; persist owned flags + bank in `meta_v0`
- [ ] Scout → +1 Free Scout / climb; Extra rumor → +1 rumor re-roll / **floor**
- [ ] Cinder Vow / Grave Nail pool-gated on unlock; combat text as table
- [ ] No midrun schema rewrite; no art / Wake / path / 2x changes

---

## Related

- Earn / Path Shop: `meta-economy-v0.md`
- Scout vs rumor wallets: `rumorcharge-v0124.md` (grant amounts for Hub-owned rows follow **this** WO)
- Mid-run: `midrun-save-v0112.md` / `save-v0112.md`
- Cards pool: `cards-v0.md` (Architect may add Cinder Vow / Grave Nail rows)

---

---

## Meta green (2026-09-24)

| Check | Status |
|-------|--------|
| Bank display | Same `remnants_bank` — UI `Remnants  N` ≡ title `Hub · N remnants` |
| Buy spend | Immediate `spendRemnants(cost)` then append unlock_id; no midrun write |
| OWNED persist | `meta_v0` unlocks set (`unlocked_cards` store) survives runs; no rebuy |
| Offer wiring | Scout `scout_charge`@8; Extra rumor `extra_rumor`@6; Host of Embers `host_of_embers`@12; Iron Lesson `iron_lesson`@12 |
| Skill gates | Pool gate on `host_of_embers` / `iron_lesson` → cards-v0 #12/#13 (Architect text) |
| Climb-save | Rem bank **read** only; midrun schema unchanged |
| Supersede | Live Hub = this 4-offer set; Pathseer@20 / Fogbreak@35 / other §3 ladder rows **not** shown |

Store already supports bank + unlock set (`MetaStore`). UI offer list + grant sites (Extra rumor **/floor**) = Engineer. Meta economy lock = **green**.

## Changelog

| Date | Change |
|------|--------|
| 2026-09-24 | v0.1.27-hub locked — four Hub offers + skill wording; Meta & Ops |
| 2026-09-24 | Meta green stamp — bank/OWNED/offer wiring confirmed vs Architect cards #12–13 |

---

## How to test (Engineer)

```bash
./gradlew :app:testDebugUnitTest --tests com.towerofdarkness.app.HubV0127Test
./gradlew :app:assembleDebug
```

Device / play:
1. Title shows `Hub · N remnants`; Hub screen title **Hub**, bank `Remnants  N`.
2. With 0 rem → all rows **Can't afford**; bank unchanged.
3. Buy Scout (8) → OWNED; next Climb Free Scout charges = 1.
4. Buy Extra rumor (6) → F1 rumor = 1; after FloorBreak→F2 rumor += 1.
5. Buy Host of Embers (12) → Cinder Vow appears in Loadout; Buy Iron Lesson → Grave Nail.
6. Kill-app / relaunch → owned flags + bank persist (`remnants_bank` + unlock ids in MetaStore).

## Glyphs (Art)

Art delivered (see `docs/art-audio/HUB_GLYPHS_v0.1.27.md`):

| Skill | Drawable |
|-------|----------|
| `cinder_vow` | `glyph_cinder_vow.png` (vow-spark motif) |
| `grave_nail` | `glyph_grave_nail.png` (nail motif) |

Wired in `SkillGlyph` / `SkillGlyphIcon`. Fidelity PLACEHOLDER per Art note — not stubbed by Engineer.

## Engineer checklist (impl)

- [x] Hub title + `Remnants  N` + 4 offers + Buy / OWNED / Can't afford + Back to title
- [x] Costs: Scout **8**, Extra rumor **6**, Host of Embers **12**, Iron Lesson **12**
- [x] Immediate bank spend; persist owned flags + bank in `meta_v0` (MetaStore)
- [x] Scout → +1 Free Scout / climb; Extra rumor → +1 rumor re-roll / **floor**
- [x] Cinder Vow / Grave Nail pool-gated on unlock; combat text as table
- [x] Art glyphs `glyph_cinder_vow` / `glyph_grave_nail` wired
- [x] No midrun schema rewrite; no art / Wake / path / 2x changes
